{% extends 'base.html.twig' %}

{% block title %} Excursion creation  {% endblock %}

{% block body %}
<div class=" col-lg-6 ">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('excursion_index') }}">Back to the list</a>
        </li>
    </ul>
</div>
{% endblock %}
